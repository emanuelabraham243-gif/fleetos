import { ShieldAlert } from "lucide-react";

import { PagePlaceholder } from "@/components/page-placeholder";

export default function ReportIssuePage() {
  return (
    <PagePlaceholder
      icon={ShieldAlert}
      title="Report Issue"
      description="The incident report form isn't built yet."
    />
  );
}
