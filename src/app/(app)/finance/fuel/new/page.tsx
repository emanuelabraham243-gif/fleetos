import { Fuel } from "lucide-react";

import { PagePlaceholder } from "@/components/page-placeholder";

export default function RecordFuelPage() {
  return (
    <PagePlaceholder
      icon={Fuel}
      title="Record Fuel"
      description="The fuel transaction entry form isn't built yet."
    />
  );
}
