import { Receipt } from "lucide-react";

import { PagePlaceholder } from "@/components/page-placeholder";

export default function AddExpensePage() {
  return (
    <PagePlaceholder
      icon={Receipt}
      title="Add Expense"
      description="The expense entry form isn't built yet."
    />
  );
}
